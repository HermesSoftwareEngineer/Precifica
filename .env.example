# Flask / App
FLASK_ENV=development
SECRET_KEY=change-me-in-dev
JWT_SECRET_KEY=change-me-in-dev
PORT=5000

# Database (use SQLite for local dev or your Postgres URL)
# Example SQLite (local file):
DATABASE_URL=sqlite:///precifica.db
# Example Postgres (Supabase):
# DATABASE_URL=postgresql+psycopg2://user:password@host:5432/dbname

# CORS / Frontend
FRONTEND_URL=http://localhost:5173

# Google APIs (optional; required for web search tool)
GOOGLE_API_KEY=

# Google Cloud / Vertex AI (optional if you use Vertex-based LLMs)
# GOOGLE_APPLICATION_CREDENTIALS=path/to/service-account.json
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id
# GOOGLE_LOCATION=us-central1
